interface Chainable {
   // login(username: string, password: string): Chainable;

   // database(action: "find", table: string, data: { id: number }): Chainable;
   // database(action: "reset", table: string): Chainable;

}